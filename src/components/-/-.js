import React from 'react';
import PropTypes from 'prop-types';
import './-.css';

const - = () => (
  <div className="-">
    - Component
  </div>
);

-.propTypes = {};

-.defaultProps = {};

export default -;
