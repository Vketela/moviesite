import React from 'react';
import PropTypes from 'prop-types';
import './NavBar.css';

const NavBar = () => (
  <nav>
    <a href="#">Home</a>
    <a href="#">About</a>
    <a href="#">Contact</a>
  </nav>
    
);

NavBar.propTypes = {};

NavBar.defaultProps = {};

export default NavBar;
