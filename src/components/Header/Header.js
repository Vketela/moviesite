import React from 'react';
import PropTypes from 'prop-types';
import './Header.css';

const Header = () => (
  <header>

    <h1>Your Logo</h1>
  </header>
);

Header.propTypes = {};

Header.defaultProps = {};

export default Header;
